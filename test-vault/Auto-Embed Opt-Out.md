---
infobox: false
title: Opt-Out Note
note: This note should NOT show an auto-embedded infobox
kind: Demo
---

Even with Auto-embed switched on, `infobox: false` is an explicit per-note opt-out, so
no box appears above this paragraph. Change it to `true` (or a template id like
`character`) to bring the box back.
